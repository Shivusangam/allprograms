
// Given a string S, print the reverse of the string after removing the vowels.
// If the resulting string is empty print '-1'.
// Input Size : 1 <= N <= 100000
// Sample Testcase :
// INPUT
// codekata
// OUTPUT
// tkdc

let a="codekata";
let c=[];

for(let i=0; i<a.length; i++){
    if(a[i]!="a" || a[i]!="e" || a[i]!="i" || a[i]!="o" || a[i]!="u")
  {
    c.push(a[i])
    
  console.log(c)
  }
  }



  
